name: signalfire

services:
  sgn-rss-fetcher:
    build:
      context: ../../../
      dockerfile: services/rss-fetcher/compose/Dockerfile
    container_name: sgn-rss-fetcher
    env_file:
      - path: ../../../.env
        required: true
    restart: unless-stopped
    networks:
      - dwh-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ../log:/app/services/rss-fetcher/log
    environment:
      - TZ=Europe/Moscow
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
    init: true
    command: ["python", "rss-fetcher.py"]
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  dwh-net:
    external: true
