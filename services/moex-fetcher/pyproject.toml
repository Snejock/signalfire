[project]
name = "moex-fetcher"
version = "0.1.0"
description = "MOEX market data fetcher"
requires-python = ">=3.14"
dependencies = [
    "shared",
    "pyyaml>=6.0.3",
]

[tool.uv.sources]
shared = { workspace = true }
